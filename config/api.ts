export const API_BASE_URL = "https://example.com/api";

// Reemplaza con la URL de tu API REST. Ejemplo final de endpoint esperado:
// GET ${API_BASE_URL}/products -> devuelve arreglo de objetos { id, name, price }
